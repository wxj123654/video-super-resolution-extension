# Video Super Resolution Extension

Browser extension for real-time video super-resolution using ONNX Runtime WebGPU.

## Skills

Before working on related code, read the corresponding skill for context and patterns:

- **Injection / popup flow** → read `.agent/skills/chrome-mv3-active-tab-injection/SKILL.md` and its `references/` files
- **Video overlay / page controller** → read `.agent/skills/html5-video-overlay-controller/SKILL.md` and its `references/` files
- **ONNX pipeline / WebGPU** → read `.agent/skills/onnxruntime-webgpu-extension/SKILL.md` and its `references/` files
